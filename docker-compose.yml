services:
  snapedit:
    build: .
    container_name: snapedit-studio
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8000
      - SNAPEDIT_BACKEND=cloud
